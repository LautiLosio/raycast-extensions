{
  "$schema": "https://www.raycast.com/schemas/extension.json",
  "name": "memory-sentinel",
  "title": "Memory Sentinel",
  "description": "Monitor process memory usage and send alerts when apps cross configured thresholds.",
  "icon": "icon.png",
  "author": "naranjax",
  "platforms": ["macOS"],
  "categories": ["Productivity", "System"],
  "license": "MIT",
  "commands": [
    {
      "name": "memory-monitor-dashboard",
      "title": "Open Memory Sentinel",
      "description": "View memory usage, manage rules, and run scans.",
      "mode": "view"
    },
    {
      "name": "background-memory-scan",
      "title": "Run Memory Scan",
      "description": "Scan running processes and send alerts for high memory usage.",
      "mode": "no-view",
      "interval": "5m"
    }
  ],
  "preferences": [
    {
      "name": "defaultThresholdGb",
      "type": "textfield",
      "required": true,
      "title": "Default Threshold (GB)",
      "description": "Alert when a process exceeds this limit unless a rule overrides it.",
      "default": "1"
    },
    {
      "name": "notificationCooldownMinutes",
      "type": "textfield",
      "required": true,
      "title": "Notification Cooldown (Minutes)",
      "description": "Minimum time before alerting again for the same process.",
      "default": "60"
    }
  ],
  "dependencies": {
    "@raycast/api": "1.104.11",
    "react": "19.0.0"
  },
  "devDependencies": {
    "@types/node": "22.13.10",
    "@types/react": "19.0.10",
    "typescript": "6.0.2"
  },
  "scripts": {
    "build": "ray build",
    "dev": "ray develop",
    "lint": "tsc --noEmit"
  }
}
